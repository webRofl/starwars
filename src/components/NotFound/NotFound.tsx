import React from 'react';
import classes from './NotFound.module.css';

const NotFound: React.FC = () => {
  return <div className={classes.notFound__wrapper}>this page not found</div>;
};

export default NotFound;
